{
  "buildCommand": "pnpm install && npx prisma generate && pnpm run build",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}